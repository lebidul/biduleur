# Instruction d'utilisation

1. faire  pip install -r requirements.txt
2. mettre les fichiers pdf à traiter dans le dossier "pdf_a_traiter"
2. python3 main.py
3. les fichiers le fichier de sortie (output.sql) sera dans le dossier "csv"
4. importer le fichier output.sql dans votre base de donnée

